## REMOVED Requirements

The `review-followup` capability is removed in full. It is a task-tracker artefact, not a behavioural specification.

### Requirement: All pre-release review tasks are completed
**Reason:** This is a meta-task ("every `- [ ]` in the affected archived task files SHALL be changed to `- [x]`"). The state it describes is recorded directly in the archived task files under `openspec/changes/archive/`; the live spec adds no enforcement. Removed as misfiled.
