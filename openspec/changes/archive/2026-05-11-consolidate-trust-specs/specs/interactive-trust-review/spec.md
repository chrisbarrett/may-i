## REMOVED Requirements

### Requirement: Screen-cleared per-rule review flow

**Reason**: Capability `interactive-trust-review` folded into `trust-command`. The per-rule review prompt is part of the unified trust CLI surface.
**Migration**: Reference `trust-command`. Body and scenarios unchanged.

### Requirement: Trusted summary line always visible

**Reason**: Capability `interactive-trust-review` folded into `trust-command`.
**Migration**: Reference `trust-command`.

### Requirement: Progress counter in HRule separator

**Reason**: Capability `interactive-trust-review` folded into `trust-command`.
**Migration**: Reference `trust-command`.

### Requirement: Pretty-printed rule forms in review

**Reason**: Capability `interactive-trust-review` folded into `trust-command`.
**Migration**: Reference `trust-command`.

### Requirement: Pretty-printed diff for changed rules

**Reason**: Capability `interactive-trust-review` folded into `trust-command`.
**Migration**: Reference `trust-command`.

### Requirement: Direct entry to review from list_status

**Reason**: Capability `interactive-trust-review` folded into `trust-command`.
**Migration**: Reference `trust-command`.
