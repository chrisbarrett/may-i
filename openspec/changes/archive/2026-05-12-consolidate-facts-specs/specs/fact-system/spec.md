## REMOVED Requirements

### Requirement: FactPattern enum available in predicates module

**Reason**: Capability `fact-system` renamed to `facts` (user vocabulary). Requirement moves verbatim to `openspec/specs/facts/spec.md`.
**Migration**: Reference `facts` instead of `fact-system`. Requirement body and scenarios are unchanged.

### Requirement: FactQuery enum available in predicates module

**Reason**: Capability `fact-system` renamed to `facts`. Requirement moves verbatim.
**Migration**: Reference `facts`.

### Requirement: FactQuery::Presence evaluates against stored facts

**Reason**: Capability `fact-system` renamed to `facts`. Requirement moves verbatim.
**Migration**: Reference `facts`.

### Requirement: FactQuery::Value evaluates as set-membership test

**Reason**: Capability `fact-system` renamed to `facts`. Requirement moves verbatim.
**Migration**: Reference `facts`.
