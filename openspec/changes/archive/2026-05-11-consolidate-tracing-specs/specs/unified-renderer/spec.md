## REMOVED Requirements

### Requirement: Heading and label widths use visible character width

**Reason**: Capability `unified-renderer` (contributor-only) folded into `trace-system`. Renderer mechanics belong with the trace surface they serve.
**Migration**: Reference `trace-system`. Body and scenarios unchanged.
