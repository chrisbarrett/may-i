## No New Requirements

This change is an internal refactoring that improves code structure without changing observable behavior (except for the minor JSON field rename from `context` to `facts`).

No new capabilities are added. No existing capability requirements are modified.
