## REMOVED Requirements

### Requirement: EvalResult exposes per-segment decisions

**Reason**: Capability `eval-segment-decisions` folded into `rule-evaluation`. Per-segment decisions are part of the evaluation contract.
**Migration**: Reference `rule-evaluation`. Body and scenarios unchanged.

### Requirement: Segment decisions describe non-overlapping byte ranges

**Reason**: Capability `eval-segment-decisions` folded into `rule-evaluation`.
**Migration**: Reference `rule-evaluation`.

### Requirement: Display does not re-evaluate to colourise

**Reason**: Capability `eval-segment-decisions` folded into `rule-evaluation`.
**Migration**: Reference `rule-evaluation`.

### Requirement: Aggregate decision unchanged

**Reason**: Capability `eval-segment-decisions` folded into `rule-evaluation`.
**Migration**: Reference `rule-evaluation`.
