## REMOVED Requirements

### Requirement: Trust listing groups by file when all trusted

**Reason**: Capability `trust-ui-listing` folded into `trust-command`. Listing is part of the unified trust CLI surface.
**Migration**: Reference `trust-command`. Body and scenarios unchanged.

### Requirement: Trust listing shows detail for untrusted programs

**Reason**: Capability `trust-ui-listing` folded into `trust-command`.
**Migration**: Reference `trust-command`.

### Requirement: Interactive approval for trust operations

**Reason**: Capability `trust-ui-listing` folded into `trust-command`.
**Migration**: Reference `trust-command`.

### Requirement: Trust listing JSON includes metadata

**Reason**: Capability `trust-ui-listing` folded into `trust-command`.
**Migration**: Reference `trust-command`.
