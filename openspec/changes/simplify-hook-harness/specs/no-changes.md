## No New Requirements

This change is an internal refactoring that removes unnecessary abstraction without changing observable behavior.

No new capabilities are added. No existing capability requirements are modified.
