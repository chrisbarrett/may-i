[lints]
workspace = true

[package]
name = "may-i-pp"
version = "0.1.0"
edition = "2024"

[dependencies]
may-i-core = { path = "../core" }

[dev-dependencies]
may-i-sexpr = { path = "../sexpr" }
proptest = "1"

# No doctests in this workspace; skip the harness build.
[lib]
doctest = false
