[lints]
workspace = true

[package]
name = "may-i-shell-parser"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
insta = "1"
proptest = "1"

# No doctests in this workspace; skip the harness build.
[lib]
doctest = false
