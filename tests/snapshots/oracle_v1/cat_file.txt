
Trace

  90: (rule (command (or "cat" …))       │
            (effect :allow               │ → :allow "Read-only access"
                    "Read-only access")) │

Result

  cat foo

  → :allow "Read-only access"

  config: ~/src/chrisbarrett/may-i/tests/fixtures/v1/config.lisp
