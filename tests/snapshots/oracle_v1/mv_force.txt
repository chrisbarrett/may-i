
Trace

  83: (rule                                   │
        (command "mv")                        │
        (args                                 │
          (if (anywhere "-f")                 │ "-f" ∈ {"-f", "a", "b"} → yes
            (effect                           │ → :ask "Force move is destructive"
              :ask                            │
              "Force move is destructive")))) │

Result

  mv -f a b

  → :ask "Force move is destructive"

  config: ~/src/chrisbarrett/may-i/tests/fixtures/v1/config.lisp
