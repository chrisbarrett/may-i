
Trace

  43: (rule (command "git")           │
            (args (and (positional    │
                         "reset")     │ "reset" = "reset" → yes
                       (anywhere      │
                         "--hard")))  │ "--hard" ∈ {"reset", "--hard"} → yes
            (effect                   │ → :ask "Dangerous git reset"
              :ask                    │
              "Dangerous git reset")) │

Result

  git reset --hard

  → :ask "Dangerous git reset"

  config: tests/fixtures/v1/config.lisp
