
Trace

  95: (rule (command "cargo")           │
            (args (or (when             │
                          ((exact)      │ → :allow
                            (effect     │ → :allow
                              :allow))) │
                      …)))              │
                                        │ [] = [] → yes

Result

  cargo --list

  → :allow

  config: ~/src/chrisbarrett/may-i/tests/fixtures/v1/config.lisp
