
Trace

                                 command │ brew list
  74: (rule (command "brew")             │
            (args (cond                  │
                    ((positional         │
                       "install")        │ "list" = "install" → no
                     …)                  │
                    ((positional         │
                       (or "list"        │ "list" = "list" → yes
                           "search"))    │ "list" = "search" → no
                     (effect :allow))))) │ → :allow

Result

  brew list

  → :allow

  config: ~/src/chrisbarrett/may-i/tests/fixtures/v1/config.lisp
