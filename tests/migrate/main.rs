// Migration integration tests: config migration, migration diff output, and
// the load graph over migrated configs.

#[path = "../common/mod.rs"]
mod common;

mod migrate_integration;
mod migrate_load_graph;
mod migration_diff;
