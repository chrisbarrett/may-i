---
source: tests/pretty_print_snapshots.rs
expression: out
---
── input (width 80) ──
(rule "rm" (if (flag "r") (ask "recursive") (allow)))
── output ──
(rule "rm"
  (if (flag "r")
      (ask "recursive")
    (allow)))
